name: Lambda Deploy - enceladus-mcp-streamable-gamma

on:
  push:
    branches:
      - v4/main
    paths:
      - "backend/lambda/mcp_streamable_gamma/**"
      - "tools/enceladus-mcp-server/server.py"
      - ".github/workflows/lambda-mcp-streamable-gamma-deploy.yml"
      - ".github/workflows/deploy-orchestration.yml"
      - ".github/workflows/lambda-deploy-reusable.yml"
  workflow_dispatch:
    inputs:
      reason:
        description: "Why this deploy is being triggered"
        type: string
        required: false
        default: "manual deploy"

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    uses: ./.github/workflows/deploy-orchestration.yml
    with:
      function_name: "enceladus-mcp-streamable"
      lambda_dir: "backend/lambda/mcp_streamable_gamma"
      deploy_script: "backend/lambda/mcp_streamable_gamma/deploy.sh"
      deploy_mode: "per-lambda"
    secrets: inherit
