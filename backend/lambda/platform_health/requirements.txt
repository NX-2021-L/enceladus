# DVP-TSK-665 — B6-R2 platform health monitor.
#
# pyarrow is the Parquet writer behind enceladus_shared.warehouse_registration,
# which publishes the two daily-grain health tables (health_surface.py). It is
# the only third-party dependency.
#
# Bounds copied deliberately from backend/lambda/governance_mart/requirements.txt:
# _build.yml installs with --platform manylinux2014_{x86_64,aarch64}
# --only-binary=:all:, so an unbounded spec would float onto the first pyarrow
# release that stops publishing a manylinux2014 wheel and hard-fail the build.
pyarrow>=17.0.0,<21.0.0

# boto3 is intentionally NOT declared: the Lambda runtime provides it.
